1. 对sql格式化的要求：
每次改完相关代码之后，必须根据 /sample 下面的测试文件，自测通过
自测通过的标准：

complex_test.sql 格式化之后，内容要跟 complex_test_target.sql 完全一致
sample_1.sql 格式化之后，内容要跟 sample_1_target.sql 完全一致
sample_2.sql 格式化之后，内容要跟 sample_2_target.sql 完全一致

注意：如果这三个“完全一致”的要求，本身就有冲突，那么请你在修改代码之前先问我，我来判断以谁为准
然后请你以文字描述的形式，整理出当前的sql格式化规则，命名为 “sql_formatter.md”，放到 /sql_formatter 下面
格式化绝对不能修改原文件的内容，只能修改 换行、缩进、空格、关键字大小写 之类的呈现方式
请新增一个功能：在sql格式化做完之后，检查前后的实际内容是否有变更，如果有变更，说明格式化失败，弹出窗口：建议手动格式化

2. 对窗口数的要求：
必须保证 publime 最多只有一个窗口。在有一个窗口的前提下，如果新开文件，则在该窗口下增加一个 tab