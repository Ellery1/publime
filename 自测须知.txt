1. 对sql格式化的要求：
请你先阅读  /sql_formatter/sql_formatter.md 里面对格式化的要求，然后自测
每次改完相关代码之后，必须根据 /sample 下面的测试文件，回归自测通过
回归通过的标准：

complex_test.sql 格式化之后，内容要跟 complex_test_target.sql 一致
sample_1.sql 格式化之后，内容要跟 sample_1_target.sql 一致
sample_2.sql 格式化之后，内容要跟 sample_2_target.sql 一致
sample_3.sql 格式化之后，内容要跟 sample_3_target.sql 一致
sample_4.sql 格式化之后，内容要跟 sample_4_target.sql 一致
sample_5.sql 格式化之后，内容要跟 sample_5_target.sql 一致
sample_6.sql 格式化之后，内容要跟 sample_6_target.sql 一致
原始myBatis日志打印.txt 格式化之后，内容要跟 处理myBatis日志之后的效果.sql 一致
原始doris-api日志打印.txt 格式化之后，内容要跟 处理doris-api日志之后的效果.sql 一致

注意：如果这几个“一致”的要求，本身就有冲突，那么请你在修改代码之前先问我，我来判断以谁为准。
然后请你以文字描述的形式，整理出当前的sql格式化规则，更新到 /sql_formatter/sql_formatter.md
格式化绝对不能修改原文件的内容，只能修改 换行、缩进、空格、关键字大小写 之类的呈现方式


**当前测试代码的宽松匹配逻辑（`normalize()` 函数）**：

| 步骤 | 操作 | 效果 |
|---|---|---|
| 1 | 去除首尾空白 | 去掉整段文本头尾的空格、换行 |
| 2 | 将所有连续空白（空格、换行、制表符）压缩为一个空格 | `SELECT  *\nFROM` → `SELECT * FROM` |
| 3 | 全部转为大写 | `select` → `SELECT` |
| 4 | 去除函数名与左括号之间的空格 | `COUNT (` → `COUNT(` |
| 5 | 去除左括号后的空格 | `( SELECT` → `(SELECT` |
| 6 | 去除右括号前的空格 | `value )` → `value)` |
| 7 | 规范化 `! =` 为 `!=` | `! =` → `!=` |

执行 normalize 后，如果实际输出和预期输出**字符串完全相等**，就算通过。

换句话说：**只要实际输出的每个 SQL 单词（关键字、表名、字段名、值）与预期一致，且出现顺序相同，就算通过。换行位置不同、缩进不同、关键字大小写不同、括号周围空格不同，都不算失败。**


2. 对窗口数的要求（代码已实现，确保不要改坏掉了就行）：
必须保证 publime 最多只有一个窗口。在有一个窗口的前提下，如果新开文件，则在该窗口下增加一个 tab